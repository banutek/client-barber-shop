export enum NotificationType {
  INFO = 'INFO',
  REMINDER = 'REMINDER',
  STATUS_CHANGE = 'STATUS_CHANGE',
}

export enum NotificationStatus {
  DISMISSED = 'DISMISSED',
  PENDING = 'PENDING',
  READ = 'READ',
}
